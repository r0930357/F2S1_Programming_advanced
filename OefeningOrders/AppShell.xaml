<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="OefeningOrders.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:OefeningOrders"
    xmlns:views="clr-namespace:OefeningOrders.Views"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        
        <Tab Title="Home">
            <ShellContent Title="Home"
                ContentTemplate="{DataTemplate views:MainPage}"
                Route="MainPage" />
        </Tab>
        
        <Tab Title="Instellingen">
            <ShellContent Title="Instellingen"
                ContentTemplate="{DataTemplate views:InstellingenPage}"
                Route="InstellingenPage"/>
        </Tab>
        
        <Tab Title="Orders">
            <ShellContent Title="Orders"
                ContentTemplate="{DataTemplate views:OrdersPage}"
                Route="OrdersPage" />
        </Tab>
        
        <Tab Title="Producten">
            <ShellContent Title="Producten"
                ContentTemplate="{DataTemplate views:ProductenPage}"
                Route="ProductenPage" />
        </Tab>
        
        <Tab Title="Werknemers">
            <ShellContent Title="Werknemers"
                ContentTemplate="{DataTemplate views:WerknemersPage}"
                Route="WerknemersPage" />
        </Tab>
        
    </TabBar>

</Shell>
